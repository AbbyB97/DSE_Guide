<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dse_guide.dseguide">

    <application
        android:name=".init"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/custom_theme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user_type_selection" />
        <activity android:name=".DSE_guide.DSE_guide_Registration" />
        <activity android:name=".DSE_guide.DSE_guide_login" />
        <activity android:name=".DSE_guide.DSE_guide_dashboard" />
        <activity android:name=".DSE_candidate.DSE_candidate_dashboard">

        </activity>
    </application>

</manifest>